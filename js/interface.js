var interface = angular.module('interface', []);

  interface.controller('InterfaceController', function(){
    this.addTask = list;
  });

  var list = {
    id: '1',
    detail: 'Walk the dog',
  };
